Place the real HoMMI deployment artifacts here:

- `model.pt`
- `model.trt.eg`
- `model.trt.eg.json`

The repository does not contain placeholder binary model files because a
zero-byte or fake model would fail at runtime and could be mistaken for a valid
deployment artifact.
